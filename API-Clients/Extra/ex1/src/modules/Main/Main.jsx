import { Link } from "react-router-dom";
import path from "../../routers/path";
function Main() {
	return (
		<p>aa</p>
		// <ul>

		// 	{Object.values(path).map((item, index) => (
		// 		<li key={index}>
		// 			<Link to={item} />
		// 			{item}
		// 		</li>
		// 	))}
		// </ul>
	);
}

export default Main;
